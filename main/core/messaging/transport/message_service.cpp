#include "core/messaging/transport/message_service.hpp"

namespace cryptalk::messaging {
int MessageService::send_message(const uint8_t* plaintext, std::array<uint8_t, 16>& receiver_id) {
    return 0;
}
}